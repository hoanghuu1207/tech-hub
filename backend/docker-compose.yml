version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: techshop_postgres
    restart: always
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-techshop}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - techshop_network

  redis:
    image: redis:7-alpine
    container_name: techshop_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - techshop_network
    command: redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: techshop_backend
    restart: always
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    volumes:
      - .:/app # Mount local folder để auto-reload source code ở dev mode
      - ../crawl/data:/app/data  # Mount crawl data để import sản phẩm
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - techshop_network

  adminer:
    image: adminer
    container_name: techshop_adminer
    restart: always
    ports:
      - "8080:8080"
    networks:
      - techshop_network

volumes:
  postgres_data:
  redis_data:


networks:
  techshop_network:
    driver: bridge
